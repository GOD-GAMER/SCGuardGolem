# Mod version for ALL targets. Per-target data (loader/SC/Java pins, ranges,
# file ids) lives ONLY in versions.matrix.toml — never here.
modVersion=2.0.0

# The addon's own CurseForge project (publishing).
curseforgeProjectId=1511550

org.gradle.jvmargs=-Xmx4G -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
